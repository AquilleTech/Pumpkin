name: Build Pumpkin for Android

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build:
    name: Build (${{ matrix.arch }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-linux-android
            arch: arm64-v8a
            clang: aarch64-linux-android28-clang
            neon: "true"
          - target: armv7-linux-androideabi
            arch: armeabi-v7a
            clang: armv7a-linux-androideabi28-clang
            neon: "true"
          - target: x86_64-linux-android
            arch: x86_64
            clang: x86_64-linux-android28-clang
            neon: "false"
          - target: i686-linux-android
            arch: x86
            clang: i686-linux-android28-clang
            neon: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        id: ndk
        with:
          ndk-version: r26c
          add-to-path: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-registry-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-registry-${{ matrix.target }}-

      - name: Cache build target
        uses: actions/cache@v4
        with:
          path: target/${{ matrix.target }}
          key: cargo-target-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-target-${{ matrix.target }}-

      - name: Generate Cargo cross-compile config
        run: |
          TOOLCHAIN="${{ steps.ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin"
          mkdir -p .cargo
          cat > .cargo/config.toml << EOF
          [target.aarch64-linux-android]
          linker = "${TOOLCHAIN}/aarch64-linux-android28-clang"
          ar = "${TOOLCHAIN}/llvm-ar"

          [target.armv7-linux-androideabi]
          linker = "${TOOLCHAIN}/armv7a-linux-androideabi28-clang"
          ar = "${TOOLCHAIN}/llvm-ar"

          [target.x86_64-linux-android]
          linker = "${TOOLCHAIN}/x86_64-linux-android28-clang"
          ar = "${TOOLCHAIN}/llvm-ar"

          [target.i686-linux-android]
          linker = "${TOOLCHAIN}/i686-linux-android28-clang"
          ar = "${TOOLCHAIN}/llvm-ar"

          [build]
          rustflags = [
            "-C", "link-arg=-Wl,--no-rosegment",
            "-C", "link-arg=-Wl,-z,noexecstack",
            "-C", "link-arg=-Wl,-z,relro,-z,now",
          ]
          EOF

      - name: Apply Android patches
        run: |
          chmod +x scripts/patch-android.sh
          NDK_PATH="${{ steps.ndk.outputs.ndk-path }}" ./scripts/patch-android.sh

      - name: Build release
        env:
          ANDROID_NDK_HOME: ${{ steps.ndk.outputs.ndk-path }}
          PKG_CONFIG_ALLOW_CROSS: "1"
          PKG_CONFIG_PATH: ""
          RUSTFLAGS: >-
            ${{ matrix.neon == 'true' && '-C target-feature=+neon' || '' }}
            -C opt-level=3
            -C lto=fat
            -C codegen-units=1
            -C panic=abort
        run: |
          cargo build --target ${{ matrix.target }} --release

      - name: Strip binary
        run: |
          STRIP="${{ steps.ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-strip"
          BIN="target/${{ matrix.target }}/release/pumpkin"
          if [ -f "$BIN" ]; then
            "$STRIP" "$BIN"
            echo "Size after strip:"
            ls -lah "$BIN"
          else
            echo "Binary not found, listing release dir:"
            ls target/${{ matrix.target }}/release/ | head -20
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pumpkin-${{ matrix.arch }}
          path: target/${{ matrix.target }}/release/pumpkin
          if-no-files-found: warn

  verify:
    name: Verify artifacts
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./builds
          pattern: pumpkin-*

      - name: Check ELF headers
        run: |
          find ./builds -type f | while read f; do
            echo "=== $f ==="
            file "$f" || true
            if file "$f" | grep -q ELF; then
              readelf -d "$f" 2>/dev/null | grep NEEDED || true
            fi
          done
